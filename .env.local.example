# SMB Owner Environment Variables

# NOTE: No API keys needed for PDF conversion!
# PDF parsing is done locally using the Monopoly library with support for 17+ banks.
# All processing happens on your server - no third-party API calls required.

# AI-Powered Merchant Categorization (Optional but Recommended)
# Get your API key from https://console.anthropic.com/
# Enables intelligent categorization of unknown merchants using Claude AI
# Example: "Starbucks" → automatically categorized as "Meals & entertainment"
# Falls back to keyword matching if not configured
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Passkey Authentication (Optional)
# Generate hash using: npx ts-node scripts/generate-passkey.ts
# See AUTH_SETUP.md for detailed instructions
# NEXT_PUBLIC_PASSKEY_HASH=your_passkey_hash_here
